Pour r‚soudre le problŠme que j'avais du genre ! [rejected] master - (non-fast-forward) error: failed to push some refs to 'https://github.com/Penda2M/Android' hint: Updates were rejected because the tip of your current branch is behind hint: its remote counterpart. Integrate the remote changes (e.g. hint: 'git pull ...') before pushing again. hint: See the 'Note about fast-forwards' in 'git push --help' for details. j'ai d— supprimer mes repositories supprimer le fichier init avec la commande git -rf .init et faire des git pull --rebase  origin master 
